'use strict';

/**
 * goal-selection-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::goal-selection-page.goal-selection-page');
