'use strict';

/**
 * blog-schedule-call router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::blog-schedule-call.blog-schedule-call');
