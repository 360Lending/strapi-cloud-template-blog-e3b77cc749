'use strict';

/**
 * goal-selection-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::goal-selection-page.goal-selection-page');
